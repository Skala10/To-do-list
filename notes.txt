план JS:

вставляем класс для создания разметки
создаем класс ToDoList

1) создаем функцию которая по клику на кнпоку add добавляет
элемент в историю ToDo.(чистит старый input) вместе с этим оно добавляет cheker слева и 
2 кнопки edit и делит справа

2) функция которая по cheker зачеркивает текст. Подключаем cheker к инпуту

3) функция которая по Edit переводит div в input и текст можно радктировать. 
И перемещает div из TODO в Completed
Подключаем кнопку Edit к div

4) функция которая по delete удаляет div из истории 

5) создаем историю для TODO и в Completed

6) Рендер выводит	 